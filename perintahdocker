Perintah Docker:

sudo docker build --tag="nama-image" .  #ganti nama-image dengan nama yang mau diberikan

sudo docker image ls -a # untuk lihat image yang sudah terbentuk

sudo docker run -td -p 5000:5000 nama-image   #-d untuk run in background, 
                                               -p adalah port setting, angka kiri adalah luar container dan angka kanan adalah dalam container
                                               port dalam container harus sama dengan setting di Dockerfile
